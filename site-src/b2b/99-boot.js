/* ==========================================================================
   Запуск кабинета: демо-агентство с балансом, клиентами и заказами во всех
   статусах. Состояние старой версии кабинета без баланса не подходит — начинаем заново.
   ========================================================================== */
"use strict";

S = loadState();
if (!S || !Array.isArray(S.ledger) || !S.agency) { S = freshState(); save(); }
render(true);
tickCharter();
setInterval(tickCharter, 1000);
